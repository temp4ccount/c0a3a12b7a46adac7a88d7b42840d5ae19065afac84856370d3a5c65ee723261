name: Unit test

on: [pull_request]

jobs:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v3

    - name: npm install
      run: npm ci

    - name: test_set
      run: npm test

